The default_no_cursor patch modifies the default cursor value for a virtual
terminal to be "no cursor". This gets rid of the blinking underline cursor
that is put up by the frame buffer after it initializes the graphics mode.
